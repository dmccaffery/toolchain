# House default for the whole fleet (read as .mise/.prettierignore in a
# consumer unless the repo carries its own .prettierignore). Patterns are
# unanchored on purpose: they are resolved against the invoking repo.

# generated by release-please; its release PRs must not trip the format gate
CHANGELOG.md

# vendored / machine-managed trees
node_modules/
.mise/
.venv/

# agent cruft
.claude/
